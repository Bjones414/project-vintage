Five polish issues resolved on the analyze report page. AnalyzeHeader now renders Make in the headline (`[Year] [Make] [Model] [Trim]`), and the redundant "Auction ended" text in the upper right is gone — the AuctionCountdown component now only mounts for live listings, leaving the upper-left sold/no-sale pill as the single status indicator. VerdictBlock no longer returns null for signed-in users with no verdict data; it renders a "Verdict in development." state with the same gold-rule treatment as the live verdict block but without a CTA. ChassisIdentityCard gains Exterior Color as its ninth field, and ColorRarityCard is restructured to a compact horizontal bar with a "Color rarity" section label (gold serif small caps) replacing the old "Exterior color" field label — the rarity dot and descriptor remain on the right. The two-column Chassis + Era grid carries an explicit `items-stretch` class to document the equal-height intent. All 246 tests pass; 16 snapshots regenerated.
