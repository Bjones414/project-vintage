All 13 color tokens from DESIGN.md (bg-canvas/surface/elevated, text-primary/secondary/tertiary/quaternary/muted, accent-primary/secondary, severity-positive/caution/concern, border-default/subtle) are now wired as CSS custom properties in `app/globals.css` (V1 light-mode values only; dark mode deferred to V1.5) and extended into `tailwind.config.ts` under grouped namespaces (bg, text, accent, severity, border), each referencing its CSS variable so dark mode can be added by swapping variables alone. The full type scale (h1–mono, 10 named sizes), 6 letter-spacing values, and borderRadius card/button tokens are also extended. Google Fonts imports for Inter, Source Serif 4 (the current variable-font successor to Source Serif Pro, used so weight 500 is available), and JetBrains Mono are added. Body defaults to Inter/bg-canvas/text-primary. No component, page, or test files were touched in this pass — components still render with their existing Tailwind classes; the cream canvas background confirms the token layer is live.
