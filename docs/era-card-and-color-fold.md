Migration `20260429020000_add_era_metadata.sql` adds four nullable TEXT columns to `porsche_generations` (`production_years`, `engine_family`, `msrp_launch_usd`, `units_produced`) and seeds verified public-record values for the 993 and 992 generations; `body_styles` for the display row is derived from the existing `text[]` column to avoid a naming conflict. EraCard is rebuilt: published generations now render italic prose followed by a 0.5px-divided 2-column metadata grid of up to six fields (Production, Body styles, Engine, Original MSRP, Units produced, Cooling), each omitted entirely when null — the empty-state for unpublished generations is unchanged. ChassisIdentityCard gains an optional `colorData` prop and renders Color Rarity as field 10 using a severity dot (green for common, amber for rare/special-order) inline with the descriptor text; the standalone ColorRarityCard is removed from the page layout but the component file is retained for the future full-report build. 247 tests pass; 16 snapshots regenerated.
