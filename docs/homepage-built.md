The homepage at `/` is now a full hero landing page: a server component (`app/page.tsx`) that fetches the Supabase user, renders TopNav, and mounts a centered hero section with a 38px serif headline ("Know what you're bidding on."), a 15px serif italic subhead, a larger URL paste field (14px, 14px vertical padding), and a "See a sample report" link to the 993 Turbo report. The interactive form lives in `components/home/HeroForm.tsx`, a client component that shares the same submit flow and four-step `AnalyzeLoadingState` used by TopNav. The old `/analyze` form page is replaced with a server-side `redirect('/')` so old links don't 404. The `analyzeUrl` unit tests are preserved in `analyze-page.test.tsx`; the obsolete AnalyzePage form tests are replaced with a single redirect assertion. Five new HeroForm tests (placeholder, example link, submit → loading, success cleanup, error cleanup) run alongside all 241 existing tests — 246 total, all passing.
