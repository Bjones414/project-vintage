Auth polish landed in this batch: TopNav now shows both "Sign in" and "Sign up" links when anonymous, with each link carrying `?next=<pathname>` when the user is on any non-home page so the sign-in flow returns them where they started. The /login and /signup pages are fully restyled to the design system — cream bg-canvas background, white bg-surface card with 0.5px border, serif headlines above the card, 10px uppercase field labels, accent-primary focus borders, dark primary button. The login server action now validates and honors the `next` param on success (same-origin only; rejects anything starting with `//`). Signup continues to fall back to `/` after email confirmation since the next-param cannot cleanly survive the email confirmation redirect without invasive changes. Supabase client now explicitly declares `persistSession: true` and `autoRefreshToken: true`. 257 tests pass.

**Dashboard TODO — refresh token lifetime:** The Supabase GoTrue refresh token expiry is not configurable via SQL or the CLI. Verify and set via: Supabase dashboard → Authentication → Settings → "Refresh token expiry (seconds)". Target value: 2592000 (30 days). If it's currently at the default of 604800 (7 days), raise it to 2592000 before the public launch.
