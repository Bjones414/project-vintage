TopNav has been fully migrated to the Project Vintage design system. The header now uses `bg-bg-elevated` (cream `#F5F2EA`) with a `border-b-[0.5px] border-border-default` rule; the wordmark renders in Source Serif 4 at 17px regular weight; the URL input uses the `rounded-button` token (2px radius), `bg-bg-surface` (white), and `focus:border-accent-primary` (gold); the avatar circle uses `bg-text-primary` fill with `text-bg-canvas` initials; the dropdown is borderline-only with no shadow and no rounded corners; all text colors reference token classes (`text-text-primary`, `text-text-tertiary`, `text-severity-concern`). Logo text was updated to "Project Vintage" and the URL field placeholder was updated to "Paste a listing URL to analyze…". All 246 tests pass.
