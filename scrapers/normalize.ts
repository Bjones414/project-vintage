// Transforms RawListing → listings insert type (canonical schema from SCHEMA.md).
// No platform-specific fields may bleed past this file into the database.
// Also used by lib/listing-parser/ for on-demand URL parsing to share normalization logic.
// Stub — implement when building first scraper.
