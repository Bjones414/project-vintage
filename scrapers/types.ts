// RawListing: the pre-normalization shape returned by each platform scraper.
// normalize.ts transforms RawListing → listings insert type before any database write.
// Stub — define when implementing first scraper.
export type RawListing = unknown;
